table_Draco
table_Draco %>%
as_gt() %>%             # convert to gt table
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "yes", # remove missing values (NAs). "yes" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>%
modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_caption("Table 1. Lizard Characteristics")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "ifany", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>%
modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_caption("Table 1. Lizard Characteristics")
table_Draco
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>%
modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_caption("Table 1. Lizard Characteristics")
table_Draco
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({se})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>%
modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_caption("Table 1. Lizard Characteristics")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>%
modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_caption("Table 1. Lizard Characteristics")
table_Draco
?modify_caption
show_header_names()
show_header_names(table_Draco)
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**bimaculatus**',
stat_2 ~ '**cornutus**',
stat_3 ~ '**melanopogon**',
p.value ~ '**p-value**'
))
modify_caption("Table 1. Lizard Characteristics")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**bimaculatus**',
stat_2 ~ '**cornutus**',
stat_3 ~ '**melanopogon**',
p.value ~ '**p-value**'
))
table_Draco
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
table_Draco
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
table_Draco
library(tidyverse)
library(gtsummary)
draco_ecomorph <- read_csv("draco_ecomorph.csv")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
library(tidyverse)
library(gtsummary)
draco_ecomorph <- read_csv("draco_ecomorph.csv")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
install.packages("gtsummary")
install.packages("gtsummary")
install.packages("gtsummary")
install.packages("gtsummary")
library(tidyverse)
library(tidyverse)
library(gtsummary)
draco_ecomorph <- read_csv("draco_ecomorph.csv")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
#show_header_names(table_Draco) #lets you preview code for changing header names and formatting
table_Draco
library(tidyverse)
library(gtsummary)
draco_ecomorph <- read_csv("draco_ecomorph.csv")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
#show_header_names(table_Draco) #lets you preview code for changing header names and formatting
table_Draco
table_Draco %>%
as_gt() %>%             # convert to gt table
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
?opt_align_table_header
?opt_align_table_caption
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
opt_align_table_header(align = "left") %>%
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
library(gt)
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
gt::opt_align_table_header(align = "left") %>%
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
#show_header_names(table_Draco) #lets you preview code for changing header names and formatting
table_Draco
table_Draco %>%
as_gt() %>%             # convert to gt table
gt::opt_align_table_header(align = "left") %>%
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table
gt::opt_align_table_header(align = "left") %>%
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table for additional formatting options and saving
as_gt(opt_align_table_header(data, align = c("left", "center", "right"))) %>%
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table for additional formatting options and saving
opt_align_table_header(data, align = c("left", "center", "right")) %>%
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table for additional formatting options and saving
opt_align_table_header(align = "left") %>%
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt()             # convert to gt table for additional formatting options and saving
#gt::opt_align_table_header(align = "left") %>%
#gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table for additional formatting options and saving
gt::opt_align_table_header(align = "left")
#gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table for additional formatting options and saving
gt::opt_align_table_header(align = "right")
#gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
table_Draco %>%
as_gt() %>%             # convert to gt table for additional formatting options and saving
gt::opt_align_caption_header(align = "left")
table_Draco %>%
as_gt() %>%             # convert to gt table for additional formatting options and saving
gt::opt_align_table_header(align = "left")
#gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
opt_align_table_header(data, align = c("left", "center", "right"))
?modify_caption
table_Draco %>%
as_gt() %>%             # convert to gt table for additional formatting options and saving
#gt::opt_align_table_header(align = "left")
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
knitr::opts_chunk$set(warning = FALSE, message = FALSE)
library(tidyverse)
library(gtsummary)
draco_ecomorph <- read_csv("draco_ecomorph.csv")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
#show_header_names(table_Draco) #lets you preview code for changing header names and formatting
table_Draco
table_Draco %>%
as_gt() %>%   # convert to gt table for additional formatting options and saving
#gt::opt_align_table_header(align = "left")
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
knitr::opts_chunk$set()
library(tidyverse)
library(gtsummary)
draco_ecomorph <- read_csv("draco_ecomorph.csv")
knitr::opts_chunk$set()
library(tidyverse)
library(gtsummary)
draco_ecomorph <- read_csv("draco_ecomorph.csv")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
#show_header_names(table_Draco) #lets you preview code for changing header names and formatting
table_Draco
table_Draco %>%
as_gt() %>%   # convert to gt table for additional formatting options and saving
#gt::opt_align_table_header(align = "left")
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
knitr::opts_chunk$set()
library(tidyverse)
library(gtsummary)
draco_ecomorph <- read_csv("draco_ecomorph.csv")
#this function works best with datasets without NA values
table_Draco <- draco_ecomorph %>%
select(species, sex, snout_vent_length_mm, perch_height_cm) %>% #only retain variables that will be used in summaries
filter(species %in% c("bimaculatus", "cornutus", "melanopogon")) %>%
tbl_summary(by = species,
missing = "no", # remove missing values (NAs). "ifany" will include a missing values summary row in table.
digits = all_continuous() ~ 1, #number of displayed digits for continuous variables
label = list(snout_vent_length_mm ~ "SVL (mm)", #change column labels in table
perch_height_cm ~ "Perch Height (cm)",
sex ~ "Sex"),
statistic = list(all_continuous() ~ "{mean} ({sd})", #can use any character for parentheses
all_categorical() ~ "{n}")) %>%
add_p(pvalue_fun = ~style_pvalue(.x, digits = 2)) %>% #number of digits displayed for p-values
modify_caption("Table 1. Lizard Characteristics") %>%
#modify_header(update = all_stat_cols() ~ "**{level}**") %>% #headers to only contain species name
modify_header(update = list(
label ~ '',
stat_1 ~ '**D. bimaculatus**',
stat_2 ~ '**D. cornutus**',
stat_3 ~ '**D. melanopogon**',
p.value ~ '**p-value**'
))
#show_header_names(table_Draco) #lets you preview code for changing header names and formatting
table_Draco
table_Draco %>%
as_gt() %>%   # convert to gt table for additional formatting options and saving
#gt::opt_align_table_header(align = "left")
gt::gtsave(filename = "draco_ecomorph_table.pdf") #save file
